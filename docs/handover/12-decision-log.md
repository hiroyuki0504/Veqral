# Decision log

確認時点: 2026-06-23 15:44:42 JST

This project did not have a formal ADR directory in the checkout. This file externalizes the decisions that are documented in `AGENTS.md`, PR notes, code, and AI context.

| ID | Decision | Date / period | Background | Adopted reason | Rejected alternatives | Impact | Revisit condition | Evidence |
|---|---|---|---|---|---|---|---|---|
| ADR-001 | Hermes native memory/session is the memory backbone | established before current handover | Veqral differentiator is cross-chat/project memory orchestration | avoids custom memory divergence and lets model engines be swapped | custom shared memory store, MCP memory layer | all memory features must read/write through Hermes-native surfaces | only if Hermes cannot support required continuity | `AGENTS.md`, `HERMES_MEMORY_INHERITANCE_PR0.md` |
| ADR-002 | Two runtime modes: direct siloed vs Hermes shared | established before current handover | Codex/Claude have native histories; Hermes has project memory | preserves native behavior and makes memory semantics explicit | forcing all runs into one shared store | UI/runtime routing, HistoryStore, Memory screen | if Codex/Claude native APIs expose safe shared project memory | `AGENTS.md`, `HistoryStore` |
| ADR-003 | Codex/Claude native histories are read-only | established before current handover | editing native histories risks corruption | safe resume/history visibility without mutation | writing/editing `~/.codex` or `~/.claude` | History viewer and direct resume only read | never revisit without tool-author guarantees | `AGENTS.md`, `HistoryStore` |
| ADR-004 | Pairing uses QR/manual fallback plus HMAC/device token | #15 and later | mobile app must safely control local Host | simple local auth with per-device token and signed pairing URL | unauthenticated LAN API, cloud account system | pairing, HMAC HTTP/WebSocket auth | if cloud account/backend is introduced | `AGENTS.md`, `MacHost/Sources/VeqralHost/main.swift`, `Veqral/AppState.swift` |
| ADR-005 | Secrets live in Keychain/env/config, not repo | ongoing | Host touches webhooks/APNs/provider keys/device tokens | reduces accidental leakage | committing `.env`/secret docs | docs and logs must redact | never for secret values | `AGENTS.md`, `VeqralRedactor.swift` |
| ADR-006 | APNs push remains off until paid Apple setup | #11/#12 onward | free personal team could not support push capability | keep real device build usable without push | forcing broken push entitlement | in-app polling/WebSocket is primary; push feature flag remains | paid Apple Developer Program and APNs key setup | `AGENTS.md`, `Veqral/Veqral.entitlements`, Host APNs code |
| ADR-007 | WebSocket reconnect replays snapshots/logs and avoids terminal resume | #20 | remote mobile networks drop | recover stream without duplicate logs or unsafe terminal replay | naive reconnect/resume always | connection strip and event stream behavior | if terminal stream protocol becomes resumable safely | `WEBSOCKET_RECONNECT_PR3.md`, app/Host code |
| ADR-008 | High-risk approvals show context before approving | #24 | mobile one-tap approval can be dangerous | commands, diffs, affected files shown before high-risk approve | approving high-risk immediately | ApprovalImpactPreview and confirmation sheet | if risk model changes | `APPROVAL_CONTEXT_PR7.md`, app code |
| ADR-009 | Portfolio real roots are explicit; sample mode is isolated | #34 | command center should not infer random user folders | prevents accidental scan/notification of private paths | broad home directory scan | missing real roots produces sample/disabled state | after real portfolio root policy is set | `PORTFOLIO_REAL_DATA_PR_A4.md`, Host config |
| ADR-010 | Sales Lab has no auto-send and Google Places discovery disabled | Sales Lab PR | outreach features carry compliance and privacy risk | user approval and ToS/key policy needed before discovery/sending | automatic email/DM or unreviewed Places scraping | manual/CSV only, generated copy/artifacts local | after legal/quota/API key policy is approved | `SALES_LAB_PR.md`, Host smoke |
| ADR-011 | Cross-vendor memory proof must use login/subscription auth, not API-key fake pass | #A7 | core claim is Hermes memory continuity across vendor engines | avoids passing with a route users cannot reproduce in Hermes login context | API-key fallback to force a pass | #A7 remains blocked until Claude/Anthropic login is Hermes-readable | after Claude/Anthropic login works | `AGENTS.md`, `HERMES_CROSS_VENDOR_PR_A7.md` |
| ADR-012 | AI-Hub policy/lane resolver owns local/subscription model selection | #47 | fixed model names drift and local/subscription availability changes | policy names survive actual model changes | hard-coded model IDs in Veqral UI/Host | Hermes control applies `Policy` from presets through resolver | if AI-Hub resolver is removed | `AGENTS.md`, `HermesControl.swift` |
| ADR-013 | AI-Hub session digest is curated, not raw history source | auto digest PR | user wants searchable continuity without replacing native histories | appends useful notes while raw histories remain native | importing all native histories into Obsidian | digest bridge skips shell and writes session notes | if AI-Hub storage policy changes | `AGENTS.md`, `AIHubSessionDigestBridge` |
| ADR-014 | Branch from `origin/main`, draft PR by default | ongoing | local branches can drift; main requires approval | keeps work reviewable and non-destructive | main direct commits/merges | current handover branch created from `origin/main` | only if user explicitly requests different flow | `AGENTS.md`, git commands |
| ADR-015 | Handoff package is `docs/handover/` plus `AGENTS.md` | 2026-06-23 | user requested externalized transfer package | structured docs separate durable facts, unknowns, risks, next actions | burying handoff in chat only | future agent/human can resume without this conversation | when docs grow stale; update per major change | user prompt, this PR |
